# This is the index of choc-readable. You should write documentation that looks
# something like this:
#
#     choc-readable = require 'choc-readable'
#     app = choc-readable(config)
#     app.run(3000)
#
module.exports = require('./src/choc-readable')
